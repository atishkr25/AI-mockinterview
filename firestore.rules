rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TODO: Replace demo access with server-enforced Clerk identity checks before production.
    match /interviews/{document} {
      allow read, write: if true;
    }

    match /userAnswers/{document} {
      allow read, write: if true;
    }
  }
}
